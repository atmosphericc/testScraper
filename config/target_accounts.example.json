{
  "_GENERATED": "THIS FILE IS GENERATED by tools/sync_config_templates.py — do not hand-edit. Secrets are placeholders (__LIKE_THIS__); every other field is the real operational value, preserved so the config can be rebuilt from scratch. Copy to the real filename, refill the placeholders, and it works.",
  "_README": [
    "Copy this file to config/target_accounts.json (which is gitignored) and fill in",
    "real credentials. One object per owned Target account. harvest_accounts.py reads",
    "this file to log each account in and write its session to `session_path`, which",
    "the existing WorkerPool then consumes (set TARGET_WORKER_POOL_SIZE=<N accounts>).",
    "",
    "Field reference:",
    "  account_id   - free-form label; also seeds this account's stable device fingerprint.",
    "  username     - Target login email.",
    "  password     - Target login password. (2FA is assumed OFF per current setup.)",
    "  session_path - where harvested cookies are written. Account 1 should keep the",
    "                 legacy 'target.json'; alts use 'target-2.json', 'target-3.json', ...",
    "                 to match WorkerPool._build_worker_configs().",
    "  profile_dir  - this account's dedicated Chrome user-data dir. Account 1 keeps",
    "                 'nodriver-profile'; alts use 'nodriver-profile-2', ... (also matches",
    "                 WorkerPool). Each account MUST have its own profile (cookies +",
    "                 fingerprint isolation).",
    "  proxy_url    - OPTIONAL. Per-account exit IP. Leave empty for home IP (Refract",
    "                 guidance: <=10 tasks often need no proxy). Bright Data ISP URLs need",
    "                 the local forwarder (src/proxy/local_forwarder.py) — see docs/MULTI_ACCOUNT.md.",
    "  timezone     - OPTIONAL. IANA tz to pin the fingerprint to the proxy's geo",
    "                 (e.g. 'America/Chicago'). Empty = derived deterministically from account_id.",
    "  enabled      - set false to skip this account without deleting it.",
    "  notes        - free text for your own bookkeeping (card/address jigging, etc).",
    "                 NOT read by any code."
  ],
  "accounts": [
    {
      "account_id": "primary",
      "username": "__ACCOUNT_EMAIL__",
      "password": "__ACCOUNT_PASSWORD__",
      "cvv": "__CARD_CVV__",
      "session_path": "target.json",
      "profile_dir": "nodriver-profile",
      "proxy_url": "",
      "timezone": "America/Chicago",
      "enabled": true,
      "notes": "Existing working account. Keep on legacy paths for zero migration. 2026-08-07: purchase exit ROTATED 31.105.133.83 -> 168.158.160.228 (reserve) — primary won 0 carts on 08-04 AND 08-06->07 (ATC 401 _ERR_AUTH_DENIED wall); IP rotation = lever #1 of the 08-07 post-mortem. Old IP parked in proxyIps.json cooling_purchase_exits, do NOT reuse for purchases. Also: 31.105.x shares a /16 with 12 of 16 sweep IPs (05-21 showed subnet-scope reputation); 168.158.160.228 mirrors business's isolated-block setup. Revert = swap the IP back. UNVALIDATED live until next boot."
    },
    {
      "account_id": "business",
      "username": "__ACCOUNT_EMAIL__",
      "password": "__ACCOUNT_PASSWORD__",
      "cvv": "__CARD_CVV__",
      "session_path": "target-2.json",
      "profile_dir": "nodriver-profile-2",
      "proxy_url": "",
      "timezone": "America/Chicago",
      "enabled": true,
      "notes": "2026-07-12 multi-account REBUILD: re-enabled for proper un-linked architecture — coherent Chrome-150 fingerprint (account_identity fixed) + login+purchase via own BD IP + keep-fresh OFF. Distinct virtual card + jigged address."
    },
    {
      "account_id": "alt-1",
      "username": "__ACCOUNT_EMAIL__",
      "password": "__ACCOUNT_PASSWORD__",
      "cvv": "__CARD_CVV__",
      "session_path": "target-3.json",
      "profile_dir": "nodriver-profile-3",
      "proxy_url": "",
      "timezone": "America/Chicago",
      "enabled": true,
      "notes": "2026-07-12 multi-account REBUILD (coherent FP + own-egress login, keep-fresh off). Third account (anuraj). 2026-06-30: assigned its own clean BD ISP IP (31.98.169.203, a reserve). The 06-24 blocker (BD-IP LOGIN + stale Chrome-130 spoof tripping Shape) no longer applies: login runs on the HOME IP (RELOGIN_SKIP_PROXY=1) and the spoof is off (TARGET_APPLY_FINGERPRINT=0), so proxy_url now affects the PURCHASE exit ONLY, exactly like primary/business. On the 06-30 drop alt-1 exited the HOME IP and ate a 24x instant-429 ATC storm. Confirm the live exit IP with: python verify_multi_account_live.py. Still needs its own distinct virtual card + jigged address. Revisit login-IP==purchase-IP coherence + per-account fingerprint after account_identity is rebuilt coherent (real Chrome 149 + host OS, no canvas tampering). To revert: set proxy_url back to empty string. 2026-08-07: purchase exit ROTATED 92.112.18.99 -> 168.158.32.64 (reserve) — alt-1 won 0 carts on 08-04 (locked 9+h) AND 08-06->07; named the prime rotation candidate once the native fallback failed, which it did (1,468 fires, 0 wins). Old IP parked in proxyIps.json cooling_purchase_exits. UNVALIDATED live until next boot."
    }
  ]
}
