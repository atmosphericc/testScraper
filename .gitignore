# Python environments
venv/
env/
ENV/
.venv/
.env/

# Python artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Session / profile data (user-specific, not committed)
target.json
target_storage.json
target_products.json
target_profile/
nodriver-profile/
playwright-profile/
chrome_user_data/
walmart-profile*/
# Resilient stack runtime state — contains live Target auth tokens
# (accessToken, idToken, refreshToken). Never commit. The *jar*.json pattern
# also catches probe-script artifacts (e.g. disabled_probe_jar.json).
state/*jar*.json
state/*_profiles/
# Per-run log files written by setup_run_logging() — large and may
# capture auth tokens via the tee'd stdout transcript.
logs/runs/

# One-off audit / probe tooling — not required by app.py or test_app.py.
# Lives locally if you need to re-run it; out of git to keep the repo lean.
probe_disabled_browser.py
logs/probe_disabled_browser_*.log

# Config with credentials
.env
config/proxyIps.json

# Logs (intentionally tracked for post-mortem of early-morning Target drops)
# logs/
# walmart/logs/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code
.claude/
# logs/api_capture.log

# Aider
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v4/

# Local chat artifacts
chat-history.json
chat.js

# Local Node tooling (Claude Code + Qwen routing — not part of the app)
node_modules/
package.json
package-lock.json
