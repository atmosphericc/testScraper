# Claims ledger

Every number that drives a bot change lives here with the script that produces it, so
anyone (a person, or a fresh-context agent that has read nothing else) can re-derive it
with one command. **Nothing is armed in `run_bot_with_nightly_restart.bat` on a claim
below VERIFIED, and every arming REM cites the claim IDs it rests on.** A refuted claim
stays here marked REFUTED so it can never be cited again.

Scripts: `tools/analysis/` (read-only parsers over `logs/runs/*.log`; never touch the bot).

## Status ladder

| status | meaning |
|---|---|
| MEASURED | one analyst, one context, ad-hoc script. Not enough to arm anything. |
| REPRODUCED | the number comes out of a committed `tools/analysis/` script, run by the author. |
| VERIFIED | an independent fresh-context re-derivation (own parser, told to refute) agrees within stated tolerance. |
| LIVE-CONFIRMED / LIVE-REFUTED | the next drop's pre-registered readout (`tools/analysis/readout_*.py`) judged it. |
| REFUTED | shown wrong; keep the row, never cite. |
| WEAKENED | verifier found the effect smaller or the attribution partly unknowable; the row says what survives. |

## Process rules (adopted 2026-09-17 evening)

1. Analyst writes the claim + script + numbers (MEASURED → REPRODUCED).
2. A separate fresh context gets only the raw logs and the claim phrased as something to refute; it must produce its own numbers (→ VERIFIED / WEAKENED / REFUTED).
3. Before the drop, the readout script for each armed change is written and committed with its pass/fail criteria (pre-registered). After the drop it is run before any narrative is written.
4. Each armed change carries exactly one grep tag so the readout can attribute outcomes.
5. At most three behavioural changes armed per drop; each with a kill-switch.

## Ledger

### C-0917-01 — the 2026-08-04 02:07:21 order was a 42 s hold, not a 2 s re-shoot
- Claim: the winning thread went fast-lane `po=429 FAST_SELLING` (02:06:28) → `HOLDING the won cart in place for 42s (pre-shot)` → one place-order POST (02:07:20) → 200 (`Checkout complete (t=47.6s)`). The FS 429 two seconds before the 200 (`HTTP 429 in 0.19s`, `In-place re-shoot 2/4 … stopping`, `clearing cart`, `DELETE cart_items/ff29f541…`) was another thread's cart. All 9 orders on 07-31 were first-POST fast-lane wins; 08-04's 02:44 and 04:11 orders were rapid in-place re-shoots after `RESERVATION_FAILURE`.
- Script: `python tools/analysis/checkout_events.py logs/runs/run_20260804_000646.log` (and `run_20260731_004924.log`); read lines 12255-12830 of the 08-04 log by hand for the thread attribution.
- Status: **VERIFIED** (fresh-context re-parse 2026-09-18 04:2x, 129 checkout POSTs across 7 logs, threads tied to `#Wn` markers): the order thread was W2/business attempt 5 (fast-lane FS at ≈02:06:35.5 - the "02:06:28" was a stale-timestamp artifact - hold 42 s, one POST, 200); the FS 2 s before belonged to W1/primary's cart (RF at 02:07:14 → FS at 02:07:18 → cleared; its DELETE later returned 200, so a different cart). All 9 07-31 orders and all 4 07-24 orders were first-POST fast-lane 200s; 08-04's 02:44 and 04:11 were RF re-shoot wins (shot 1 at +3.5-4 s; shot 3 at +3-5 s gaps); all four 08-04 orders were business.
- Consequence: commit 541f8742's evidence sentence is wrong; the R5 cadence stays armed on C-0917-04 instead (docs/HOT_SKU_FIX_2026_09_16.md §12a).

### C-0917-02 — primary's edge-limiter passes come from the first shot after an idle, and our own shot density was high on 09-16
- Claim (pass = any ATC outcome but an empty-body edge 429; 401s excluded): primary on hot TCINs, by gap since its own previous shot on the same TCIN — first shot / >200 s: **6/11** (09-11) and **1/7** (09-16) = 7/18; 40-80 s: 0/12 and 0/90 = **0/102**; <10 s: 0/1 and 0/28; 10-40 s: 1/1 and 3/51. Our own shots (any account) on the TCIN in the 120 s before each primary shot: median **3** on 09-11 (primary 7/27 overall) vs **7** on 09-16 (primary 4/180). Corrections vs the first write-up: the medians were first reported as 2 and 9 (the 9 double-counted alt-1's 401 lines).
- Script: `python tools/analysis/edge_pass_by_gap.py logs/runs/run_20260911_010759.log logs/runs/run_20260915_233355.log --ident primary`
- Status: numbers **VERIFIED** (±2 shots; fresh-context re-parse 2026-09-18 04:1x), causal reading **REFUTED**. Corrections: the 7/18 counts a 431 (05:19:15) and a 10.4 s 503 (02:10:52) as passes - strict (DCO/201 only) it is 5/18; 09-11 primary is 7/27 loose, 5/27 strict (never 7/20); business 09-11 is 0/20 non-401, 09-16 0/172. The pass rate is NOT monotonic in idle time (10-40 s: 4/53; 40-80 s: 0/102; 80-200 s: 0/6), so the gap bins were a proxy for shot TYPE - see C-0917-08. Density: between nights the direction holds (09-16 median 7 vs 09-11 median 3, per-shot rate ~10x lower) but WITHIN 09-16 density does not predict passing (0-2: 1/9, 3-5: 0/13, 6-9: 1/114, 10+: 2/45; the three mid-night passes were at density 9-10), and per hour in stock the nights are alike (1.8/h vs 2.2/h strict passes): the 10x per-shot drop is mostly denominator (90 of primary's 181 shots were 0/90 re-entries). Between-night confounds on record: a0 sensor replay (yes on 09-11, no on 09-16), different SKUs, alt-1's 401 share.
- Consequence: the business park stays justified only by C-0917-05/07 (cheap, not proven beneficial); readout R3 now also splits race-start vs re-entry.

### C-0917-08 — the wave-first solo cold re-entry never passes; race-start shots take the passes (from the flip/density verifier)
- Claim: on 09-11 + 09-16, strict passes (DCO/201) by shot type, non-401 denominators: attempt-1 (race-start) shots **8/267**, attempt-2 (the 55-70 s solo cold re-entry) **1/273** - and that one (09-11 05:19:54, the 201) was a re-shoot 38 s after a 431 whose attempt-1 never reached the limiter, so effectively 0/272; primary alone 0/181. The re-entry has the LONGER own-gap and no sibling shots, so neither "cold" nor "alone" makes a shot pass. Race-start passes came from flips (09-11 4/12, 09-16 1/11) and from level-rearm re-publishes mid-window (09-16 3/80) at own-gap 13-26 s and density 9-10. Secondary lead (small n): primary's race-start pass rate by how many of OUR race-start shots reached the limiter (non-401) in that race: 1 → 2/8, 2 → 4/29, 3 → 2/67 - consistent with our own volley self-competing for a per-TCIN bucket, inseparable from "only the home IP passes".
- Script: the verifier's (`scratchpad/verify_flip/parse_shots.py`, `analyze.py`, `deeper.py`); to port to `tools/analysis/attempt_split.py`. Readout R3 already reports race-start vs re-entry.
- Status: **VERIFIED** (independent parser). Mechanism unknown: what differs between a race-start shot and a re-entry shot in OUR code (header-set source/age, bank-gate freshness, warmup-tab dummy POST at race start) is the next thing to measure; the DCO-burst re-POSTs (C-0917-03: 30% at 4-8 s after a DCO) show a 2nd/3rd shot CAN pass, so it is the 60-s solo re-entry specifically that is dead.
- Not acted on tonight: removing the re-entry (`TARGET_WAVE_REENTRY_MIN_S=999` makes 1-shot races) would let the 3 s level re-arm fire race-starts every ~6-8 s - dense, unmeasured; pacing that needs a per-TCIN min-gap in `app.py _level_rearm_loop` that exempts held carts (code). Candidate for the drop after next, with the readout's race-start-vs-re-entry line as the evidence.
- Armed on it: business parked on the hot TCINs (`TARGET_PARK_ACCOUNT_TCINS`) — provisional; revert = delete the `;business:` half.

### C-0917-03 — after an ATC-level DCO_RATE_LIMITED 429, a re-POST within 5 s gets back through the edge limiter; waiting 45-90 s does not
- Claim: all logs, next shot on the same (identity, TCIN) after a DCO 429: <5 s: **15/48** past the edge (3 × 201, 12 × DCO again); 5-15 s: 0/5; 15-45 s: 1/3 (a 201); 45-90 s: **0/7**. Identity-tagged logs only (attribution certain): <5 s: **4/8** past the edge (all 4 DCO again, no 201); 45-90 s: **0/7**. The three 201s are July pairs on 1011483406 whose same-thread attribution is unknowable from the untagged log (two have 0.0 s gaps).
- Script: `python tools/analysis/dco_next_shot.py logs/runs/run_2026*.log`
- Status: **VERIFIED on the mechanism, WEAKENED on the conclusion** (fresh-context re-parse, 2026-09-18 03:5x; trellis thread attribution calibrated 99.1% on the tagged logs; its own clock from the per-thread `t-left` counter and ms `[ATC_RESP]` stamps because 65/105 DCO shots sit inside >5 s timestamp gaps).
  - Confirmed: same-thread re-POST within ~4-8 s of a DCO reached the carts service (non-edge, non-401) **15/50 (30%)** on strictly-attributed pairs, 17-20/87 (20-23%) including consistency-determined ones; control after an empty-body edge 429: **0.35-1.2%** (1.4-4.3% inside the same races). The 3 × 201 survive strict attribution (07-16 L25258, 07-27 L16258, 08-04 L12048), plus a 4th (08-04 02:06:16, primary-or-business) and a ladder 201 (07-31). The DCO is a shared open moment, not a per-identity token: other identities' same-moment shots were DCO/201 16.5% of the time vs 0.8% after an edge trigger.
  - Weakened: the "45-90 s → 0/7" half cannot be separated from "September hot SKU" - all seven are 09-11/09-16 policy re-entries, and September 30-120 s re-entries after *edge* triggers also went 0/257. Every DCO→201 cart was on an ordinary SKU (1011483414, 1011483406: 7-11% edge rates); the pattern is untested on 85%-edge hot SKUs. **Zero orders ever came from a DCO→201 cart** (all died at place-order FS / checkout_busy) - which is the leg R5 (C-0917-04) now addresses.
  - Net: the burst buys a ~20-30% draw at the carts service where the old policy bought ~0; whether that draw turns into a hot-SKU cart and then an order is what readout R2/R4 will say.
- Armed on it: `TARGET_ATC_DCO_BURST=1` with `TARGET_ATC_DCO_BURST_MAX=2` (cut from 6 after the fresh-context code review: density + bank depth, see C-0917-06) — provisional; kill `TARGET_ATC_DCO_BURST=0`.

### C-0917-04 — same-cart checkout POSTs after a FAST_SELLING rejection
- Claim: gap ≤10 s: 11 POSTs, 1 past the gate (07-21 04:45:21, +7 s → 424 RESERVATION_FAILURE), 0 orders (all July, CVV era, regular SKUs); gap 26-69 s: 11 POSTs, 4 past the gate (07-31 04:56:19; 08-04 02:07:20 = the order; 08-04 02:57:16; 08-04 02:59:38), 1 order; September hot SKUs (09-11, 09-16): 0/8 at 26-51 s, and 4 of 09-16's 6 fired after the item was gone. "RF,RF,RF → FS on the 4th" holds 2/3 (08-04 04:11 went RF,RF,RF → 200).
- Script: `python tools/analysis/checkout_events.py <log>` for each of run_20260720_231634, 20260723_235455, 20260727_231922, 20260731_004924, 20260804_000646, 20260911_010759, 20260915_233355; attribution by hand.
- Status: **VERIFIED with corrections** (same verifier): ≤10 s same-cart re-POSTs after an FS: **13**, 1 past the gate (07-21 04:45:21 → 424 RF), 0 orders; 26-69 s: **13**, 4 past the gate (07-31 04:56:19 RF; 08-04 02:07:14 RF [missing from my list]; 08-04 02:07:20 = the order; 08-04 02:57:16 RF), 1 order - my 4th instance (08-04 02:59:38) does not qualify (first POST on a fresh cart, no-key 424 = OOS). September: 0/6 same-cart re-POSTs at 45-51 s, 0/8 POSTs total. "RF,RF,RF→FS" is 3/4 (07-31 02:29:37 was missing), but FS mostly hits the 1st POST on a cart (9 of ~16), i.e. it is demand state, not a per-cart counter. The `400 body=101` responses are checkout POSTs against a cart the preceding 424 had emptied. **No FS → fast re-shoot → 200 exists in any log; the only post-FS conversion is the +45 s one.**
- Armed on it: R5 won-cart cadence (`TARGET_WONCART_STEADY_GAP_S=5`) — provisional. Honest reading of the verified numbers: per POST the 45 s hold gets past the gate more often (4/7 in Jul/Aug regular; 0/6 Sept hot) than a rapid re-POST (1/13), but per unit of live window the two are a wash (≈15 draws at ~8% vs ≈2 at 0-57%), and only the fast cadence can catch an opening that lasts seconds. It stays armed for that reason alone; readout R4 decides. Revert = `SCHEDULE_S=5,15` + `STEADY_GAP_S=45` + `MAX_TICKETS=14`.

### C-0917-05 — business never passed the edge limiter on a September hot TCIN
- Claim: business non-401 shots on hot TCINs: 0/172 on 09-16 (plus 10 × 401), 0/20 on 09-11 (plus 6 × 401); every flip shot fired in the same instant as primary's and lost on arrival order.
- Script: `python tools/analysis/edge_pass_by_gap.py <log> --ident business`
- Status: **VERIFIED** (fresh-context re-parse, 2026-09-18 03:4x: business 0/192 hot in Sept, 0/94 volley, 0/98 re-entry; never a 201 in 1,168 shots on four nights). WEAKENED on the *mechanism*: primary's volley pass rate is 2/28 (7.1%) when business fired in the previous 3 s and 6/76 (7.9%) when it did not; 8 of primary's 9 Sept passes came with business having fired within ±25 ms. So the park is cheap (forfeits ≈0.4 expected DCO passes per night, ≈0 carts) but its benefit to primary is unmeasurable in this data; the next-drop readout R3 is the arbiter. Counter-argument on record: business was the ONLY passer in late Aug (9 DCO passes, 08-26/08-28) when primary was also BD-proxied and slower, so a parked business is zero hot-SKU coverage if primary is wedged/held/relaunching during a drop.

### C-0917-07 — the edge limiter admits at most one of our shots per volley, to the fastest path (from the park verifier)
- Claim: in 1,732 same-volley pairs (two identities on the same TCIN within 3 s) across 08-25/08-27/09-11/09-16 there was never a double pass. On every night the identity with the lowest median edge-429 round trip was the only identity that ever passed: business in late Aug (0.19 s vs primary/alt-1 0.29-0.30 s, all three on BD exits; primary 0/592), primary in Sept (0.15-0.16 s from the home IP vs business 0.20-0.26 s; business 0/192). Fire-order deltas were ±25 ms; the ~50 ms one-way path difference decides arrival.
- Script: the verifier's own (`scratchpad/verify_park/parse_shots.py`, `analyze.py`, outputs `rounds.jsonl`, `analysis_report.md`); to be ported to `tools/analysis/volley_pairs.py`.
- Status: **VERIFIED** (independent parser; the 08-25 night has no per-response timestamps and is excluded from the order test).
- Consequence: a second identity only adds a draw if it fires in a *different* volley. Combined with C-0917-03 (the edge re-admits a shot 1-5 s after a DCO), a per-account dispatch stagger of a few seconds on hot TCINs (business fires ~5-6 s after primary, i.e. after primary's burst) is the candidate that turns business from a wasted same-volley shot into an independent draw. Not built; needs a drop's worth of `[DCO_BURST]` data on the refill first.

### C-0917-06 — the 09-09 census density gradient, re-derived in-repo
- Claim: over every fast-lane shot since July (5,387 non-401 shots, all identities, all logs), by our own prior shots on the TCIN in the preceding 120 s: prior 0 → 201 rate 6.0% / edge-pass 13.3% (n=249); 1-2 → 10.1% / 20.2% (n=109); 3-4 → 2.1% / 5.3% (n=189); 5-8 → 1.2% / 2.3% (n=486); 9+ → 0.1% / 0.8% (n=4,354). (The 09-09 census said ≥5 → 0/438 vs ~10% cold; same shape.)
- Script: `python tools/analysis/census_density.py logs/runs/run_2026*.log`
- Status: **REPRODUCED**. Caveat: prior 0-2 largely means "the first wave at a flip", so timing and our own density are confounded here too; the prescription (fire at flips, do not pile our own shots on a live TCIN) is the same under both readings. On regular SKUs the other identities' flip shots (prior 1-2) convert at ~10%, which is why the park is hot-TCIN-only.

### First live read-out — run_20260917_232400 (2026-09-17/18), `tools/analysis/readout_next_drop.py`
- C-0917-05/07 (park): **LIVE-CONFIRMED as specified** — business 0 shots on the 13 parked TCINs, 21 sit-outs; on the three un-parked semi-hot TCINs all three accounts fired and only primary passed (2 DCO), consistent with one-token-per-volley.
- C-0917-03 (burst): **LIVE-CONFIRMED on the mechanism** — 7 bursts, 1 of 6 re-POSTs back through the edge (DCO again), 0 carts from a burst. Cost surfaced: primary's first-ever home-IP 401s (8, one TCIN, ten minutes, ~18 attempts) — the attempt-count wall applies to the home IP too.
- C-0917-02/08 (density / race-start): primary strict 5/42 (11.9%) vs 2.2% on 09-16; race-start 3/19, re-entry 2/23 (two re-entry passes against the 1/273 baseline — small n). INCONCLUSIVE on the pre-registered criterion (4/37 on the parked list); direction favourable.
- C-0917-04 (R5 cadence): **LIVE-CONFIRMED that the gate opens under a 5 s cadence** — cart 1011483413: pre_checkout 201 at +9 s, RESERVATION_FAILURE at +18 s and +39 s (real reservation attempts, 218/292 ms). 0 orders: both reservations failed at qty 2 and the 424 emptied the cart. Cart 1011960739's line was evicted by Target 13 s after the add.
- NEW C-0918-01 — a place-order 424 RESERVATION_FAILURE empties the cart, and on a hot SKU the FS gate hides it (pre_checkout answers 429 before the cart is read): cart 1 fired 30 tickets and two held re-entries at an empty cart while the TCIN was back in stock. Status: **MEASURED live (n=1) + the verifier's 5/5 Jul/Aug 424s**. Fix armed: `TARGET_WONCART_EVICTION_READ=1` (docs #14).
- NEW C-0918-02 — reservation at qty 2 failed twice at the instant the gate opened. Status: **MEASURED (n=2)**; qty 1 pinned on the 13 hot TCINs (`TARGET_QTY_PER_TCIN=1`). Readout for the next drop: any RESERVATION_FAILURE at qty 1 vs a 200.

### Second pass on run_20260917_232400 (2026-09-18 evening) — `tools/analysis/windows.py`; verified by two fresh-context agents (their scripts and outputs: `logs/analysis_2026_09_18/v_carts`, `v_atc`)
Parser note: `tools/analysis/shots.py` was rebuilt after this pass (identity-keyed outcome matching + legacy-path shots); it now equals the interceptor's per-POST count on all three September logs (138 / 544 / 80). My first totals for this night (61 primary shots) were a stale 03:50 snapshot: **primary fired 78 shots — 59 edge 429, 7 DCO 429, 2 × 201, 8 × 401, 2 × 424**; business 30, alt-1 30, neither on a parked TCIN.

- C-0918-01 — the won-cart loop kept firing at a dead cart. Status: **VERIFIED in substance** (v_carts): after the 424 at ticket 8 (+38.9 s) there were 32 more tickets (26 × 429, 6 × 400), no 2xx, two held re-entries whose cart GET returned 429, and primary fired zero ATCs from 02:40:50 to 02:48:10 through two live windows of the TCIN. "The cart was empty" is an inference until +439.8 s (first successful cart GET, `lines=0`), but every 400 was a gate-open answer on a cart that had answered 201 at ticket 2, and all six 424→next-POST sequences in the 07-31/08-04 logs returned 400. The `pre_400_streak` exit never fired because every pre 429 resets the streak. Fix armed: `TARGET_WONCART_EVICTION_READ=1` + `TARGET_WONCART_EVICTION_PRESUME=1` (read, retry once, unreadable twice = evicted; stale held marker with an unreadable cart = normal shot).
- C-0918-02 — both real reservation attempts were for qty 2. Status: **VERIFIED** for the quantity (`cart_qty=2` in Target's own 201 body); **NO evidence why they failed** (bodies were not logged; `[FS_TICKET_BODY]` added). New from the verifier: **a 429 RESERVATION_FAILURE is retryable** (08-04 L88710-89077: three of them 2.6-2.9 s apart, then the order) and **a 424 RESERVATION_FAILURE is terminal** (6/6 → 400, never an order). Cart A was still winnable after the +17.5 s RF; its next three draws over 21 s hit the gate (one after a 7.5 s jitter gap). qty 1 stays pinned (cheap; unproven).
- C-0918-03 — both 201s were wave-first cold re-entries (fired 66.9 s and 57.8 s after a rejected shot; window age 74.5 s and ~121 s). Status: **VERIFIED**. Caveat: cold re-entries still had the lowest edge-pass rate of any trigger class that night (3/34). The re-entry stays.
- C-0918-04 — flip shots. Status: **VERIFIED with a better cut**: 4 of 12 flips passed the edge (33%; 4/10 excluding two 401 flips), none became a cart; the stronger predictor is FIRST CONTACT with a TCIN that night (first-ever own shot 4/8 vs every later shot 7/70, p=0.012; flips of later windows on an already-shot TCIN 0/4).
- C-0918-05 — burst re-POST signing. Status: **WEAKENED**: 5 page-signed (0/5 past the edge) and 2 banked (1/2), not 6 and 1 — a 1-second-old banked set still drew an edge 429. All night banked 11/68 vs page-signed 0/10 (p=0.34), and every page-signed shot was a quick-follow, so signing is confounded with shot position (banked quick-follows 4/23 vs page-signed 0/10, p=0.29). Root cause of the unsigned re-POSTs (verifier): the bank refills only when a slot frees, idle captures land every ~85 s, so primary holds two replayable sets only 21-26% of the time; no freshness gradient up to the 100 s cap (0-10 s 4/20, 11-30 s 2/20, 31-60 s 3/23, 61-100 s 2/5). No replay older than 100 s exists in any log, so the cap stays (Stellar's default expiry is 300 s — open question). The banked-burst rule stays armed as cheap and plausible, not proven.
- C-0918-06 — coverage holes. Status: **VERIFIED**; no other window all night was empty or late (every first shot fell in the same second as the app's in-stock edge; sweeps 2.9-3.1 clean reads/s, no 403).
- C-0918-07 — the 401 streak. Status: facts **VERIFIED**, my attempt-wall cause **REFUTED as stated**: 14 attempts on 1011960739 in 5 min 38 s preceded it, but 17 attempts on 1011407490 drew zero 401s and the proxied accounts drew a 401 on shot #1; token death is refuted for the streak (dummy write probes answered 424 "write-auth alive" throughout, so the deny was TCIN-scoped); "just sold out" and "the 40 checkout POSTs" are not supported. Unique to primary on that TCIN: a won cart that Target evicted plus 5 admitted ATCs. Cause OPEN. The first 401 (02:49:12) followed a write-auth death and token re-mint by 19 s — and **primary's write-auth died 13 times overnight** (business 5, alt-1 4), twice inside live windows; `[AUTH_CRITICAL] TOKEN CHURN` was logged. Primary's cart GETs were 429-throttled 10 of 14 times from 02:43:40 to 02:57:28 (other accounts 11/11 OK): the 40-ticket storm is the likely cause (inference).
- C-0918-08 — checkout timing (from v_carts). Status: **VERIFIED**: both carts got ONE gate draw in their first 3 s (the chain's own pre_checkout, FS at +0.1-0.3 s), then waited 3.36 s / 3.13 s for ticket 1; 4 draws in the first 15 s of each; ~22% of gate-facing requests passed (10/46); cart B's line lived ≤ 12.4-13.6 s and no place-order was ever fired on it; across every log none of the 8 carts whose chain pre_checkout was FS-rejected converted, and all 14 fast-lane-era orders were a single chain finishing in ~3 s. Armed on it: schedule floor 2 → 1 s, `TARGET_WONCART_SCHEDULE_S=1,1,1,2,2,3`, `STEADY_GAP_S=3`, `JITTER_S=1`.
- C-0918-09 — quiet mode leaked on cart B (from v_carts). Status: **VERIFIED**: a harvest run in flight when the cart was won clicked the fulfillment cell at +3.9 s and did a real (client-blocked) Add-to-cart click at +5.3-6.0 s; no evidence of harm. Armed: `TARGET_HARVEST_QUIET_RECHECK=1`.
- Also verified absent all night: MULTI_SKU_MISS, PX challenge, captcha, a real wedge, logged_in=False, AMBIGUOUS_COMMIT, any relaunch/nav/CVV PUT/DELETE on primary during either cart. Minor: 6 primary shots were fired into already-closed windows (a cold re-entry does not re-check stock); the loop's `live=` lags the stock monitor by its 20 s hysteresis.

### Adversarial code review of the change set (2026-09-20, fresh context; notes in `logs/analysis_2026_09_18/review_code/`)
Verdict: **not blocking** — no traced path places a second order or buys more than Q. Six substantive findings, all fixed the same evening, each mutation-checked (7/7 mutants caught):
- **F1 (MAJOR)** the burst's bank wait polled `harvest_set_ready()` with margin 0 while the caller re-checked with margin 5, so the whole `95 < age <= 100 s` dead band — exactly the 09-18 02:05:14 case the margin was added for — returned "not ready" at t=0 and skipped its wait. Fix: `harvest_wait_for_set(max_wait_s, margin_s=0.0)`, caller passes its margin (with a `TypeError` fallback for an older executor).
- **F2 (MAJOR)** `gate_kind == 'dco'` is set for ANY non-empty ATC 429 body, not the demand throttle, so the burst could fire on the per-TCIN EDGE class (our own volume; 0 orders from 13,244 re-POSTs). Fix: the 429 result now carries `gate_body` (a 160-char head) and the burst additionally requires `DCO_RATE_LIMITED` / `FAST_SELLING` / `high demand`; `TARGET_ATC_DCO_BURST_ANY_BODY=1` restores the looser test, and a result with no body head keeps prior behaviour.
- **F3 (MAJOR, money-adjacent, was already contained)** a PRESUMED eviction leaves the line with no marker and no dirty flag; the reviewer traced the containment (strict gate sums all lines of T → `cart_qty_over` → QG deletes; `verified` can never be True on a ledger built from a stacked cart) and confirmed **no place-order can fire on qty > Q**. Fix as recommended: presume without the dirty flag only when both reads failed with **429** (Target throttling the read); any other failure (status 0, evaluate error = our tab) presumes AND marks the cart dirty so the next purchase deletes the line before adding.
- **F4 (MINOR)** the pre-400 eviction read fired on ANY pre 400, including payment/CVV ones. Fix: `_pre400_is_evictionish()` — keyless or reservation/cart-shaped keys only.
- **F5 (MINOR)** `woncart_read_has_tcin` treated a 2xx whose body carried no `cart_items` array as proof of an empty cart. Fix: the read JS reports `has_items`, `_cart_items_read` propagates it, and proof requires it.
- **F6 (MINOR)** the burst sleep could fall under `TARGET_CART_HOLD_CHECK_INTERVAL_S`, in which case `_check_cart_hold` returns without reading and a silently landed add would be missed. Fix: the burst sleep is floored at that interval.
- **F10 (NIT, judgement call)** the qty-1 rationale cited cart A (1011483413) although that SKU is neither pinned nor parked. Resolved by correcting the rationale, NOT by extending the pin: 1011483413 belongs to the `1011483xxx` family that produced **every order this bot has ever placed, all at qty 2**; pinning it on n=2 with no evidence the quantity mattered would halve units on the only family that converts. The pin stays limited to the 13 hot TCINs, where we have never converted at any quantity.
- Verified safe by the review: QG force-on; no deadlock in the bank wait; `_dco_burst_waited` never stale; flag-off equivalence holds; AC-1 latch and the R3 dirty release not bypassed; bat CRLF-only, ASCII, no `% ! | & < > ^` in any new REM line, every changed variable assigned exactly once and last, all values inside the code's clamps.

### Older claims still relied on (not re-derived tonight)
- Limiter identity table (`logs/analysis_2026_09_16/wf2/limiter_identity_table.md`): BD hot-SKU passage 3.4% (Jul) → 1.6% (mid-Aug) → 0/331 (Sept); home 9/206. Status: MEASURED + 31 verifier agents on the 09-16 forensics (see that folder).
